# macOS
.DS_Store

# 편집기
.vscode/
.idea/

# 로그
*.log

# 공개 저장소에 올리지 않는 내부 참고 자료
# (앱 동작과 무관 — 로컬에는 그대로 남습니다)
DESIGN.md
teamboard-prototype.html

# ⚠ config.js 는 일부러 무시하지 않습니다.
#   GitHub Pages가 이 파일을 브라우저로 서빙해야 앱이 동작합니다.
#   여기에는 공개돼도 안전한 anon 키만 넣으세요(service_role 키 금지).
